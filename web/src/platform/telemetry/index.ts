export function recordConsoleEvent(_name: string, _attributes: Record<string, string | number | boolean> = {}) {
  // Reserved platform boundary for future frontend telemetry without coupling features to a vendor SDK.
}
